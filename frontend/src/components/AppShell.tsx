import type {ReactNode} from 'react';
/** Provide navigation, theme switching, and logout for authenticated views. */ export function AppShell({page,setPage,children,onLogout}:{page:string;setPage:(p:string)=>void;children:ReactNode;onLogout:()=>void}){const links=['Dashboard','Transfers','Beneficiaries','Cards','Statements','Support'];return <div className="shell"><aside><b>ansh<span>bank</span></b>{links.map(x=><button key={x} className={page===x?'nav active':'nav'} onClick={()=>setPage(x)}>{x}</button>)}</aside><main><header><div><small>PERSONAL BANKING</small><h1>{page}</h1></div><div><button className="icon" onClick={()=>{const t=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=t;localStorage.theme=t;}}>Theme</button><button className="icon" onClick={onLogout}>Sign out</button></div></header>{children}</main></div>}
