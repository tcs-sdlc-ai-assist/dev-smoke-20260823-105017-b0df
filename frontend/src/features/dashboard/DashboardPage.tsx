import {useEffect,useState} from 'react';import {api} from '../../api/client';import type {Account} from '../../types';import {Card} from '../../components/Ui';
/** Show the customer's live account overview. */ export default function DashboardPage(){const[a,setA]=useState<Account[]>([]);useEffect(()=>{api.get('/accounts').then(r=>setA(r.data))},[]);return <><div className="hero"><small>TOTAL AVAILABLE</small><h2>${a.reduce((s,x)=>s+x.balance,0).toLocaleString(undefined,{minimumFractionDigits:2})}</h2><p>Across {a.length} accounts · updated just now</p></div><h3>Your accounts</h3><div className="grid">{a.map(x=><Card key={x.id}><small>{x.type.toUpperCase()}</small><h3>{x.name}</h3><p>{x.number}</p><strong>${x.balance.toLocaleString(undefined,{minimumFractionDigits:2})}</strong></Card>)}</div></>}
