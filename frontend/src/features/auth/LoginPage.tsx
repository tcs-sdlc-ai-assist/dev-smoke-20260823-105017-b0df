import {useState} from 'react'; import {api} from '../../api/client'; import {Button,Card} from '../../components/Ui';
/** Sign a demo customer into the live backend. */ export default function LoginPage({onLogin}:{onLogin:()=>void}){const[email,setEmail]=useState('demo@ansh.bank'),[password,setPassword]=useState('demo123'),[error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();try{const r=await api.post('/auth/login',{email,password});localStorage.token=r.data.token;onLogin()}catch{setError('Check your demo credentials and try again.')}};return <div className="login"><Card><small>WELCOME TO</small><h1>ansh<span>bank</span></h1><p>Your money, in a clearer light.</p><form onSubmit={submit}><label>Email<input aria-label="Email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input aria-label="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p role="alert">{error}</p>}<Button type="submit">Sign in securely</Button></form><small>Demo: demo@ansh.bank / demo123</small></Card></div>}
