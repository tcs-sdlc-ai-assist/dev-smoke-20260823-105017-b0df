package com.ansh.bank;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Start the Ansh Bank development application. */
@SpringBootApplication
public class AnshBankApplication {
  /** Launch the web server. Args: command-line values. */
  public static void main(String[] args) { SpringApplication.run(AnshBankApplication.class, args); }
}
